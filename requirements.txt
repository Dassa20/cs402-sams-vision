# SAMS Vision Core Dependencies
PyQt6>=6.5.0
opencv-python>=4.8.0
numpy>=1.24.0
scikit-image>=0.21.0
matplotlib>=3.7.0
seaborn>=0.12.0
pytest>=7.4.0

# Optional: enables OCR-based detection of handwritten absence notation (e.g. "ab") so it
# isn't misread as a signature. Also requires the Tesseract-OCR system binary
# (https://github.com/UB-Mannheim/tesseract/wiki on Windows) -- the app runs fine without
# either; this check is skipped automatically when they're not installed.
pytesseract>=0.3.10
